// Paste your class server address between the quotes, for example:
//   window.KALIMA_PROXY = "https://kalima.yourname.workers.dev";
// Leave it empty ("") to keep using each person's own Gemini key.
window.KALIMA_PROXY = "";
